To run experiments.py use this command:
python experiments.py
After the run for each itertation the train and test accuracy and loss will be printed along with the time it took to run the training process.